import 'package:flutter/material.dart';

class NoSearchProductView extends StatelessWidget {
  const NoSearchProductView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
